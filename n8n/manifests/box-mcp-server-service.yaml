apiVersion: v1
kind: Service
metadata:
  labels:
    service: box-mcp-server
  name: box-mcp-server
  namespace: n8n
spec:
  type: ClusterIP
  ports:
    - name: "8000"
      port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    service: box-mcp-server