apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loki-pv-claim
  namespace: grafana
spec:
  storageClassName: kube-nfs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Gi