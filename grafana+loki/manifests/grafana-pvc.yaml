apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-pv-claim
  namespace: grafana
spec:
  storageClassName: kube-grafana
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi