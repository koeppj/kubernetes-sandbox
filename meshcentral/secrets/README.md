# MeshCentral Secrets

Copy `config.json.sample` to `config.json` and replace the placeholder Keycloak client secret before running `scripts/deploy.sh`.

The live `config.json` is used to create the `meshcentral-config` Kubernetes Secret and should not be committed.
